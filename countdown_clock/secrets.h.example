#pragma once

// Copy this file to secrets.h and fill in your real credentials.
// secrets.h is gitignored and must never be committed.

#define WIFI_SSID "YourSSID"
#define WIFI_PASS "YourPassword"

// Optional backup access point (e.g. a mobile hotspot), tried automatically
// if the primary SSID above isn't in range. Uncomment both lines to enable -
// leave commented out to connect to only the primary network.
// #define WIFI_SSID2 "YourBackupSSID"
// #define WIFI_PASS2 "YourBackupPassword"
